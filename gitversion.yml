workflow: TrunkBased/preview1
tag-prefix: 'v'
branches:
  main:
    regex: ^master$
    label: ''
  feature:
    regex: (?i)^(feature|feat|fix|bugfix)[\/-](?<BranchName>.+)
    label: alpha.{BranchName}