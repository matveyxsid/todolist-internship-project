# gitversion.yml
workflow: TrunkBased/preview1
commit-message-incrementing: Enabled

branches:
  main:
    regex: ^master$
    label: ''
    increment: Patch
    is-main-branch: true

  feature:
    regex: ^(feature|feat|fix|bugfix)/(?<BranchName>.+)
    label: alpha
    increment: Inherit
    source-branches: [main]
